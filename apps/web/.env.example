# Public app name exposed to the client (safe to use in browser UI)
NEXT_PUBLIC_APP_DEFAULT_NAME="Garage"

# Public API base URL used by the frontend (must be accessible from the browser)
NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1